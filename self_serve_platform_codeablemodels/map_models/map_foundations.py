from codeable_models import CClass
from metamodels.guidance_metamodel import category, pattern

foundations_category = CClass(category, "Foundations Category")

api_description = CClass(pattern, "API Description")

